export type Enum = {
  index: number;
  name: string;
};

export type Pkg = {
  index: number;
  name: string;
  lineupIndex: number;
};

export type Version = {
  index: number;
  name: string;
  indexPrev: number;
};
